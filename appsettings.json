{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  },
  // these may be overridden by either user secrets or environment variables at run time
  // the for the key use the format SECOptions:MongoConnectionString
  "SECOptions": {
    "MongoConnectionString": "mongodb://localhost:27017",
    "DatabaseName" : "secdata"
  }
}
